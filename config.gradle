ext {
    android = [
            //编译SDK的版本
            compileSdkVersion: 23,
            //Build Tools的版本
            buildToolsVersion: "24.0.0",
            //应用支撑最小SDK版本
            minSdkVersion    : 14,
            //应用目标SDK版本
            targetSdkVersion : 23,
    ]
}